image: node:latest

pipelines:
  branches:
    master:
      - step:
          caches:
            - node
          script:
            - yarn
            - yarn lint
            - yarn tsc
            - yarn build
            - cp build/index.html build/200.html
            - yarn deploy
