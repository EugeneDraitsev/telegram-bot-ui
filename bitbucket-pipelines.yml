image: alekzonder/puppeteer:latest

pipelines:
  branches:
    master:
      - step:
          caches:
            - node
          script:
            - yarn global add surge
            - yarn
            - yarn lint
            - yarn tsc
            - yarn build
            - surge build telegram-bot-ui.surge.sh
